dbs:
  - path: /data/db.sqlite3
    replicas:
      - type:     sftp
        host:     ${LITESTREAM_URL}
        user:     ${LITESTREAM_USER}
        password: ${LITESTREAM_PASSWORD}
        path:     ${LITESTREAM_DIR}